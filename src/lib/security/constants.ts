/** How long a misuse cooldown lasts after the model flags abuse. */
export const COOLDOWN_MS = 72 * 60 * 60 * 1000;

/** Reset strike count after this much time without a new misuse flag. */
export const STRIKE_RESET_MS = 72 * 60 * 60 * 1000;
