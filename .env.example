# OpenAI API key (required). Get one at https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# Optional: override the default chat model id (defaults to gpt-5.4-nano)
# CHAT_MODEL=gpt-5.4-nano
